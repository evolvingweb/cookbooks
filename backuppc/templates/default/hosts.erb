#============================================================= -*-perl-*-
#
# Host file list for BackupPC.
# Generated by chef for <%= node[:fqdn] %>
#

host        dhcp    user    moreUsers
<%- @hosts.each do |host| %>
<%= "#{host[:fqdn]}  0  #{node[:backuppc][:server][:user]}" %>
<%- end %>
